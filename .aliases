if [[ "$(uname)" == "Linux" ]]; then
    alias ll='ls -lh --color'
    alias la='ls -lah --color'
else
    alias ll='ls -lhG'
    alias la='ls -lahG'
fi

alias cp='cp -i'
alias mv='mv -i'
alias ..='cd ..'
alias grep='grep --color=always'
alias hist='fc -li 0';
alias python=python3
